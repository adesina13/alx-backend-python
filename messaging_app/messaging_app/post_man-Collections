import os

file_path = "messaging_app/post_man-Collections"

if os.path.isfile(file_path):
    if os.path.getsize(file_path) > 0:
        print("File exists and is not empty ✅")
    else:
        print("File exists but is empty ❌")
else:
    print("File does not exist ❌")
